<p id="notice"><%= notice %></p>

<form>
    <%# Verifica os labels do formulário %>
    <div class="form-group">
      <%= label_tag nil, @coin.class.human_attribute_name(:description) %>
      <%= text_field_tag nil, @coin.description, class: "form-control", disabled: true %>
    </div>

    <div class="form-group">
      <%= label_tag nil, @coin.class.human_attribute_name(:acronym), style: "display: block" %>
      <%= text_field_tag nil, @coin.acronym, class: "form-control", disabled: true%>
    </div>

    <div class="form-group">
      <%= label_tag nil, @coin.class.human_attribute_name(:url_image), style: "display: block" %>
      <%= text_field_tag nil,  @coin.class.human_attribute_name(:url_image), class: "form-control", disabled: true %>
    </div>

    <div class="form-group">
      <%= label_tag nil,  @coin.class.human_attribute_name(:mining_type) %>
      <%= text_field_tag nil, @coin.class.human_attribute_name(:description), class: "form-control", disabled: true %>
    </div>
    <br>
</form>
 
<%= link_to t('links.edit'), edit_coin_path(@coin), class: "btn btn-primary"%> |
<%= link_to t('links.back'), coins_path, class: "btn btn-primary" %>